version: '3.1'

services:
    webserver:
        image: rdonnarumma/airflow-gcp:latest
        restart: always
        ports:
            - "8080:8080"
        environment:
            - EXECUTOR=Local
        command: webserver
        #healthcheck:
        #test: ["CMD-SHELL", "[ -f /usr/local/airflow/airflow-webserver.pid ]"]
        #interval: 30s
        #timeout: 30s
        #retries: 3
